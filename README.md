Extract the file

Make sure both files remain in the same directory.

First Just run the file using the bash file provided in another terminal.

What it does is simply log in as the root user for full privileges and compile and run a C file (i.e., Keylogger file)

It'll run in the background and will record all keystrokes. 
